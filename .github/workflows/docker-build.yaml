name: Containerize
on:
  pull:
    branches:
      - main


jobs:
  Docker-build:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - name: Build Image
      run: docker build ./uri-shortner/ -t Stefanie-A/uriimage:latest
