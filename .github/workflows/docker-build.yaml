name: Containerize
on:
  push:
    branches:
      - main


jobs:
  Docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Image
        run: docker build ./python-uri-shortner/ -t uriimage:latest
