name: Lint test
on:
  [workflow_dispatch]
  # push:
  #   branches:
  #     - main
  # pull_request:
  #   branches:
  #     - main


jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: checkout
        run: echo "Successful!"